--- src/components/Drawers/EquipmentDrawer.tsx
+++ src/components/Drawers/EquipmentDrawer.tsx
@@ -56,7 +56,8 @@
     const handlePointerDown = (e: React.PointerEvent) => {
-        const target = e.target as HTMLElement;
-        if (target.closest('.inline-btn')) {
+        const targetNode = e.target as Node;
+        const target = (targetNode.nodeType === 3 ? targetNode.parentElement : targetNode) as HTMLElement;
+        if (target?.closest('.inline-btn')) {
             handleLogPointerDown(e);
             return;
         }
@@ -69,7 +70,8 @@

     const handlePointerUp = (e: React.PointerEvent) => {
-        const target = e.target as HTMLElement;
-        if (target.closest('.inline-btn')) {
+        const targetNode = e.target as Node;
+        const target = (targetNode.nodeType === 3 ? targetNode.parentElement : targetNode) as HTMLElement;
+        if (target?.closest('.inline-btn')) {
             handleLogPointerUp(e);
             return;
         }
@@ -91,7 +93,8 @@

     const handleClick = (e: React.MouseEvent) => {
-        const target = e.target as HTMLElement;
-        const btn = target.closest('.inline-btn') as HTMLElement;
+        const targetNode = e.target as Node;
+        const target = (targetNode.nodeType === 3 ? targetNode.parentElement : targetNode) as HTMLElement;
+        const btn = target?.closest('.inline-btn') as HTMLElement;

         if (btn) {
             handleLogClick(e);
--- src/components/Drawers/InventoryDrawer.tsx
+++ src/components/Drawers/InventoryDrawer.tsx
@@ -77,7 +77,8 @@
     const handleClick = (e: React.MouseEvent) => {
-        const target = e.target as HTMLElement;
-        const targetBtn = target?.closest('.inline-btn.auto-item');
+        const targetNode = e.target as Node;
+        const target = (targetNode.nodeType === 3 ? targetNode.parentElement : targetNode) as HTMLElement;
+        const targetBtn = target?.closest('.inline-btn.auto-item');

         if (targetBtn && activeTab === 'quests') {
@@ -94,8 +95,9 @@

     const handlePointerDown = (e: React.PointerEvent) => {
-        const target = e.target as HTMLElement;
-        if (target.closest('.inline-btn') || target.closest('.drawer-tab')) {
-            if (target.closest('.inline-btn')) handleLogPointerDown(e);
+        const targetNode = e.target as Node;
+        const target = (targetNode.nodeType === 3 ? targetNode.parentElement : targetNode) as HTMLElement;
+        if (target?.closest('.inline-btn') || target?.closest('.drawer-tab')) {
+            if (target?.closest('.inline-btn')) handleLogPointerDown(e);
             return;
         }
@@ -106,8 +108,9 @@

     const handlePointerUp = (e: React.PointerEvent) => {
-        const target = e.target as HTMLElement;
-        if (target.closest('.inline-btn') || target.closest('.drawer-tab')) {
-            if (target.closest('.inline-btn')) handleLogPointerUp(e);
+        const targetNode = e.target as Node;
+        const target = (targetNode.nodeType === 3 ? targetNode.parentElement : targetNode) as HTMLElement;
+        if (target?.closest('.inline-btn') || target?.closest('.drawer-tab')) {
+            if (target?.closest('.inline-btn')) handleLogPointerUp(e);
             return;
         }
@@ -127,9 +130,10 @@

         const dx = Math.abs(x - startX);
         const dy = Math.abs(y - startY);

-        const btn = target.closest('.inline-btn') as HTMLElement;
+        const btn = target?.closest('.inline-btn') as HTMLElement;

         if (btn && dx < 10 && dy < 10) {
             handleLogClick(e as unknown as React.MouseEvent);
-        } else if (!target.closest('.drawer-tab')) {
+        } else if (!target?.closest('.drawer-tab')) {
             if (activeTab === 'info') {
                 if (Date.now() - (e.currentTarget as any)._lastTap < 300) {
